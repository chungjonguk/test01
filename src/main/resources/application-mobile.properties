# 모바일 실기기 테스트 프로필 (spring.profiles.active=mobile)
# 같은 Wi-Fi에서 http://<PC-IP>:8081 로 접속

server.address=0.0.0.0

# DevTools — PC에서 파일 저장 시 실기기 새로고침 (LiveReload 확장 필요)
spring.devtools.livereload.enabled=true
