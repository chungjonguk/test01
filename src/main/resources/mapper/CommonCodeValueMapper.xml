<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.springbootapp.mapper.CommonCodeValueMapper">

    <resultMap id="commonCodeValueResultMap" type="CommonCodeValue">
        <result property="codeId" column="code_id"/>
        <result property="codeVal" column="code_val"/>
        <result property="useYn" column="use_yn"/>
        <result property="regId" column="reg_id"/>
        <result property="regdateDt" column="regdate_dt"/>
        <result property="updateId" column="update_id"/>
        <result property="updateDt" column="update_dt"/>
    </resultMap>

    <sql id="commonCodeValueColumns">
        code_id, code_val, use_yn, reg_id, regdate_dt, update_id, update_dt
    </sql>

    <select id="findByCodeId" resultMap="commonCodeValueResultMap">
        SELECT <include refid="commonCodeValueColumns"/>
        FROM common_code_value
        WHERE code_id = #{codeId}
        ORDER BY code_val
    </select>

    <select id="findByCodeIds" resultMap="commonCodeValueResultMap">
        SELECT <include refid="commonCodeValueColumns"/>
        FROM common_code_value
        <where>
            <if test="codeIds != null and !codeIds.isEmpty()">
                AND code_id IN
                <foreach collection="codeIds" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
        </where>
        ORDER BY code_id, code_val
    </select>

    <select id="findByCodeIdAndCodeVal" resultMap="commonCodeValueResultMap">
        SELECT <include refid="commonCodeValueColumns"/>
        FROM common_code_value
        WHERE code_id = #{codeId}
          AND code_val = #{codeVal}
    </select>

    <insert id="insert">
        INSERT INTO common_code_value (code_id, code_val, use_yn, reg_id, regdate_dt, update_id, update_dt)
        VALUES (#{codeId}, #{codeVal}, #{useYn}, #{regId}, #{regdateDt}, #{updateId}, #{updateDt})
    </insert>

    <update id="update">
        UPDATE common_code_value
        SET use_yn = #{useYn},
            update_id = #{updateId},
            update_dt = #{updateDt}
        WHERE code_id = #{codeId}
          AND code_val = #{codeVal}
    </update>

    <delete id="deleteByCodeId">
        DELETE FROM common_code_value
        WHERE code_id = #{codeId}
    </delete>

    <delete id="deleteByCodeIds">
        DELETE FROM common_code_value
        WHERE code_id IN
        <foreach collection="codeIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteByCodeIdAndCodeVal">
        DELETE FROM common_code_value
        WHERE code_id = #{codeId}
          AND code_val = #{codeVal}
    </delete>
</mapper>
