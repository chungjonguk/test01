<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.springbootapp.mapper.DashboardCompanyConfigMapper">

    <resultMap id="dashboardCompanyConfigMap" type="DashboardCompanyConfig">
        <id property="companyId" column="company_id"/>
        <result property="hiddenJson" column="hidden_json"/>
        <result property="regId" column="reg_id"/>
        <result property="regDt" column="reg_dt"/>
        <result property="updateId" column="update_id"/>
        <result property="updateDt" column="update_dt"/>
    </resultMap>

    <select id="findByCompanyId" resultMap="dashboardCompanyConfigMap">
        SELECT company_id, hidden_json, reg_id, reg_dt, update_id, update_dt
        FROM dashboard_company_config
        WHERE company_id = #{companyId}
    </select>

    <insert id="insert">
        INSERT INTO dashboard_company_config (company_id, hidden_json, reg_id, update_id)
        VALUES (#{companyId}, #{hiddenJson}, #{regId}, #{updateId})
    </insert>

    <update id="update">
        UPDATE dashboard_company_config
        SET hidden_json = #{hiddenJson},
            update_id = #{updateId},
            update_dt = CURRENT_TIMESTAMP
        WHERE company_id = #{companyId}
    </update>
</mapper>
