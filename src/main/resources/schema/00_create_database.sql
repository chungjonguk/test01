-- DB 생성 (root 또는 CREATE 권한 계정으로 1회 실행)
-- PostgreSQL에서는 Spring 연결 시 DB가 이미 지정되므로 아래는 비활성화합니다.
-- CREATE DATABASE IF NOT EXISTS spring_boot_app
--   CHARACTER SET utf8mb4
--   COLLATE utf8mb4_unicode_ci;

-- USE spring_boot_app;
